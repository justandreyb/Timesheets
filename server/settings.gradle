pluginManagement {
    repositories {
        maven { url "http://repo.maven.apache.org/maven2" }
        maven { url "https://plugins.gradle.org/m2/" }
        maven { url 'http://repo.spring.io/libs-release' }
    }
}

rootProject.name = 'timesheets.server'

include 'core', 'model', 'database'
